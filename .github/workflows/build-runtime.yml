# Build and test BoxLite core runtime.
#
# This workflow provides fast feedback on Rust changes:
# - Runs cargo build and test on 2 platforms
# - Triggers on push/PR to core Rust code
#
# Note: SDK workflows (build-wheels, build-node) are self-contained
# and trigger independently on release.
name: Build Runtime

on:
  push:
    branches: [main, develop]
    paths:
      - 'boxlite/**'
      - 'boxlite-shared/**'
      - 'guest/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-runtime.yml'
      - '.github/workflows/config.yml'
  pull_request:
    branches: [main]
    paths:
      - 'boxlite/**'
      - 'boxlite-shared/**'
      - 'guest/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-runtime.yml'
      - '.github/workflows/config.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  config:
    uses: ./.github/workflows/config.yml

  build:
    name: Build Runtime (${{ matrix.os }})
    needs: config
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Use simple 2-platform matrix (same as SDK workflows)
        os: ${{ fromJson(needs.config.outputs.build-targets) }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: ${{ needs.config.outputs.rust-toolchain }}

      # Cache Rust dependencies
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "runtime-v2"
          cache-on-failure: true

      # Install platform dependencies
      - name: Install platform dependencies
        run: make setup

      - name: Build BoxLite runtime
        run: make runtime

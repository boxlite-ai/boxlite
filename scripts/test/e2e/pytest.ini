[pytest]
testpaths = cases
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
markers =
    e2e: tests that hit the full SDK→API→Runner→VM path
    ssh: CA-based real SSH over the direct tunnel; needs BOXLITE_E2E_SSH_ENABLED
